<h1 class="text-center"><%= __('title.tags') %></h1>
<ul class="text-muted top-gap-big">
  <% site.tags.sort('-length').forEach((tag) => { %>
    <li>
      <a href="<%= url_for(tag.path) %>">
        <%= tag.name %>
      </a>
      <small>(<%= tag.length %>)</small>
    </li>
  <% }) %>
</ul>